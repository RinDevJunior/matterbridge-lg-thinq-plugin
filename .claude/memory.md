# Project Shared Memory

Durable knowledge store. Read at session start **only** by: `technical-architect`, `investigator`, `implementer`, `reviewer`, `test-writer` (and `wiki-manager` as a gather-mode source). Other agents skip it.
It is version-controlled — commit and push changes so teammates can pull the latest knowledge.

**Pruning rules:** max 10 bullets per section, **max 2 lines per bullet**. When adding would exceed the cap, move the oldest/least-durable entry to `wiki/memory-archive.md` (never delete outright). `reviewer` enforces the caps whenever it appends. Compress wording — keep file paths and hard facts, drop rationale prose.

---

## Architecture Insights

<!-- Patterns and relationships discovered during analysis -->

- V1 room-detection flip-flop root cause (confirmed Aug 3, 2026, real S8 log + floor-plan photo): `MapInfoListener.tryParseV1MapBinary` (`mapInfoListener.ts:211-238`) resolves+publishes a room from EVERY independent V1 map push with zero temporal smoothing (`onV1RoomResolved`→`AreaManagementService.setV1ResolvedSegment`, unconditional overwrite). Rc05's containment tiers (PR #154) are correct per-snapshot; the miss is one layer up. Floor plans where one room (e.g. a corridor) borders most others amplify this: `resolveCurrentRoom`'s tier-3 `findNearestSegment` (bounding-box center, not true centroid) flips between different neighbors on tiny pose noise near that hub. Fix: 2-consecutive-match confirmation gate added to `setV1ResolvedSegment` (`v1PendingResolution` map) — see `workspace/room-detection-flip-flop-investigation/plan.md`.
- Live map updates: `resolveInitialAreas` must sync-bootstrap via `fetchAndApplyMapInfo`/`fetchAndApplyRoomMap` (ignore V2); public `getMapInfo`/`getRoomMap` stay V2-only when `liveMapUpdates`. `handleActiveMapChanged` must intersect with Matter `supportedAreas` before writing `selectedAreas`.
- Multi-map areas: partial room-map fetch/push merges by `mapId` via `mergeSupportedAreasByMap` (keeps other maps, re-indexes areaIds); full `mapInfo.allRooms` / V1 map-info push stays full replace.
- Multi-map + `enableMultipleMap`: ON → bootstrap all physical maps via `switchMap`+sync fetch, merge by mapId; OFF → primary only (`maps[0]`) via `getSupportedAreas(..., false)` — wired from `configManager.isMultipleMapEnabled`.
- No code path calls V1 `dispatcher.getHomeMap` (`get_map_v1`) on a recurring basis — `PollingService` only calls `getDeviceStatus`; `AreaManagementService.startPeriodicRefresh` only calls `getMapInfo` (`get_multi_maps_list`). A V1 position-based fallback needs its own explicit trigger.
- `MapInfoListener.tryParseB01MapBinary` skips `Protocol.map_response` when `deviceProtocol === ProtocolVersion.V1` (`mapInfoListener.ts:165`), but a SEPARATE `tryParseV1MapBinary` (`:211-238`) handles V1 map binaries via `decryptAndUnzipV1Map`→`resolveCurrentRoom`→`onV1RoomResolved`; confirmed firing correctly in real S8 logs (Jul 25, corrects a prior stale claim that V1 binaries were dropped entirely).
- `OperationStatusCode.WashingTheMop`(23) override (`stateResolver.ts:128-139`) writes `operationalState=FillingWaterTank` for ~2-3min at clean-start (real mop-pad wash phase) before `Running` — Apple Home master tile likely shows this as "Preparing" (unconfirmed, HomeKit-side); `RvcRunMode.currentMode`/`RvcCleanMode.currentMode` update correctly/promptly throughout, independent clusters.
- V1 `mapParser.ts` `resolveCurrentRoom`: exact-pixel-match tier only matches pixels where `parseImageBlock` recorded a segment — misses on wall/obstacle/type-0 pixels, common at room boundaries. Fallback tiers: (1) bounding-box containment check (single-match-only, Jul 28 merged), (2) nearest-centroid distance. **Jul 28:** `LegacySegmentInfo` now carries `boundingBox` (raw pixel-space); `findContainingSegment` private method inserted in `resolveCurrentRoom` between exact-pixel and `findNearestSegment`, returns single segment only (0 or 2+ → falls through to centroid). No shared point-in-rect helper (`legacyIslandDetection.ts` only has rect-rect `boxesOverlap`).
- V1 `"rr"`-frame block types beyond this repo's known 4 (charger=1, image=2, robot=8, cleaned=11): `FORBIDDEN_ZONES=9` (no-go zone), `VIRTUAL_WALLS=10`, `NO_MOP_ZONE=12` — same buffer, same `get_map_v1` RPC, no new RPC needed (corroborated byte-for-byte by ioBroker.roborock + roborock-gitlab, independent authors). Wall payload = 2 (x,y) endpoints/8 bytes; zone payload (9 and 12 share one decoder) = 4 corners/16 bytes; count field is `u32` at block offset `+0x08`, entries start at `dataPosition+hlength`. Coordinate units inferred as raw mm (same space as robotPosition), medium confidence only — no real captured buffer with these blocks populated exists anywhere checked (this repo or the 2 corroborating OSS repos). See `workspace/virtual-wall-detection-feasibility/answers-virtual-wall.md`.

- Greenfield repo confirmed Sep 8, 2026: `matterbridge-lg-thinkq-plugin` had no `src/`/`package.json` at task start — `wiki/reference-workspaces.md` created this cycle (4 rows: matterbridge-example-dynamic-platform, homebridge-lg-thinq, homebridge-webos-tv, matterbridge-roborock-vacuum-plugin) for future investigator/wiki-manager lookups.

## Known Patterns

<!-- Coding patterns established in this project -->

- **Commit messages:** `<type>(<optional scope>): <short summary>` + optional why-body. Types: `feat`, `fix`, `docs`, `chore`, `refactor`, `test`, `style`, `perf`. Imperative mood; no `Co-Authored-By`.
- Per-device override pattern: array of objects with key (`serialNumber`) + value (`productName`), gated by a boolean in `advancedFeature.settings`. See `DeviceProductNameOverride` (`RoborockPluginPlatformConfig.ts:58`).
- New config sections in schema use JSON Schema `if/then` blocks under `advancedFeature.allOf`.
- **Agent frontmatter:** `effort`/`maxTurns` after `color`, before `tools`; `tools` MUST be a comma-separated string — YAML block lists silently fail and the subagent falls back to Read/Write/Edit/Bash only.
- **Subagent toolsets (macOS, re-verified Jul 10 2026):** native `Glob`/`Grep` absent from Task subagents — `mcp__glob-grep__*` is the only working search. `LSP` newly reachable in wildcard-tools subagents via deferred ToolSearch (one probe; explicit `tools:` lists historically never got it) — don't rely on it in agent definitions.
- **Subagent task tracking:** `TaskCreate`/`TaskUpdate`/`TodoWrite` never reach Task subagents (verified Jul 10 2026, direct-executor probe) — never list them in agent frontmatter; EM tracks pipeline steps in the main session, subagents report plain text (use a `progress.md` file in the task folder if live status is ever needed).

## Decisions Made

<!-- Architectural and design decisions with rationale -->

- LG ThinQ+webOS migration (Sep 8, 2026, greenfield `matterbridge-lg-thinkq-plugin`): two device families, one platform — `services/thinq/`+`platform/thinq/` (cloud EMP auth+REST+poll) and `services/webos/`+`platform/webos/` (local SSAP websocket) share only `module.ts` entrypoint + one namespaced `node-persist` instance. Phased roadmap (Phase1=ThinQ AC only, Phase2=rest of ThinQ, Phase3=webOS, Phase4=MQTT/deferred, Phase5=ThinQ1-legacy/deferred). See `workspace/migrate-lg-thinq-to-matterbridge/plan.md`.
- Matter's `OperationalState`/`OvenMode`/`OvenCavityOperationalState`/`TemperatureControl` clusters (via `matterbridge/devices` classes `LaundryWasher`/`Dishwasher`/`Oven`/`MicrowaveOven`) cleanly replace homebridge-lg-thinq's `Valve`/`Television`+`InputSource` HomeKit idiom (no native HomeKit washer/oven service existed) — confirmed live in `matterbridge-example-dynamic-platform/src/module.ts:2599-3000`.
- webOS TV maps to Matterbridge `BasicVideoPlayer`+`Speaker` device classes (`MediaPlayback`/`KeypadInput` command handlers), not a custom composition — confirmed via `matterbridge-example-dynamic-platform/src/module.ts:2783-2816`.
- `MatterbridgeServiceAreaServer.selectAreas` (`@matterbridge/core`) unconditionally calls `super.selectAreas(request)` with the ORIGINAL request AFTER our command handler runs, writing `this.state.selectedAreas` directly (bypasses `updateAttribute`). Any empty-input resolution must intercept in `RoborockServiceAreaServer.selectAreas` and forward a RESOLVED request to `super.selectAreas`, not `updateAttribute` from `roborockVacuumCleaner.ts`'s command handler — that write always loses the race.
- `extra_time` → `estimatedEndTime` removed (Jul 2026); replacement: V1-only opt-in `enableEstimatedEndTime` (default off) + `clean_time`/`clean_percent` linear ETA in `src/share/estimatedEndTime.ts`; B01/Q7/Q10 stay `null`.
- `robot.homeInFo.activeMapId` inits to `-1` (`deviceConfigurator.ts:105`), only written via B01/Q7 `onActiveMapChanged` — V10/V1 NEVER updates it. `RoomIndexMap.getAreaId(roomId,mapId)` then always misses for V10/V1 multi-map; `getAreaIdV2(roomId)` is the correct fallback.
- No "currently selected map" Matter attribute exists — Apple Home infers active map from which map's rooms appear first in `supportedAreas`/`selectedAreas` (mirrors `roborockService.ts:350` `buildCleanCommand`).
- `handleActiveMapChanged` (`serviceAreaHandler.ts:234-275`) has no idle/cleaning guard — unconditionally overwrites `selectedAreas`/`currentArea`/`progress`; only matters if the device reports a genuinely different active map mid-clean.
- Shared state-update helper pattern (`applyResolvedStateUpdates`, `deviceStateHandler.ts:22-49`): extract identical Promise.all/snapshot/completion blocks into a private async helper; callers keep their own state resolution and return values.
- `AreaManagementService.supportedRoutines` (routine-as-room, `mapId=999`) is structurally separate from `supportedAreas` — "all rooms of active map" logic from `getSupportedAreas` excludes routines automatically.
- `homeInFo.activeMapId` alone is unreliable for active-map inference (stays -1 for V10/V1). Fallback order used in `SELECT_AREAS` empty-list handling: Matter `selectedAreas` mapId → `activeMapId` (if not -1) → first `supportedAreas` mapId.
- `RvcRunMode.ChangeToMode(Idle)` is now handled via `IdleModeHandler` (new, Jul 10 2026) → `roborockService.pauseClean`. `Mapping` deferred: `AbstractMessageDispatcher` (V10/Q7/Q10) has zero mapping/explore-start command.
- `getSupportedAreas()` (`initialData/getSupportedAreas.ts`) is the single point enforcing "Areas non-null mapId ⇒ supportedMaps non-empty" (Matter `#assertSupportedAreas`). `processValidData` branch always assigns numeric `mapId`; empty `mapInfo.maps` (e.g. `MapInfo.empty()` fallback) previously left `supportedMaps=[]` — fixed via `buildPlaceholderSupportedMaps` (pair, don't null — matches existing `createFallbackArea` convention).
- V1 currentArea fallback (planned): CLI's `extractNamedRooms`/`roomDisplayName` (`cli/mapListHelpers.ts`) stay CLI-only — runtime resolves `segmentId → areaId` via existing `roomIndexMap.getAreaId`/`getAreaIdV2`, doesn't need room names, so no move/share needed.
- `progress` reset-on-new-clean (Jul 13, 2026, revised): TWO reset triggers — `selectAreas` AND `handleServiceAreaUpdate`'s Idle branch (replaces old Operating→Completed flip with direct `[]` clear, since both writes happen in one sync call with no observable gap). NOT `RoborockService.startClean` — its `HandlerContext` chain never carries `robot`/`device`.
- `RoborockVacuumCleaner.roborockService` exposed as `public readonly` (not private) to enable behaviors/servers (e.g., `RoborockServiceAreaServer`) to call `setProgress()`/`getProgress()` directly without routing through platform. Companion to existing public readonly `device` and `homeInFo` fields.
- Dock→Matter mapping (Jul 2026): default bundle Items 1+2+3+4 in `DockStationStatus.ts` only; Item 3 vs 5 mutually exclusive (full dss vs dustBag-only); codes 32/33/35 as `DockErrorCode` enum; dss priority clearWater→dirty→dustBag→cleanFluid→filter→updown.
- `progress` reset on start-of-clean (Jul 14, 2026): 3rd trigger added — `handleServiceAreaUpdate` compares stored-vs-current `operationalState` "actively cleaning" classification (Docked/Stopped/Error = not cleaning), NOT raw `CLEANING_STATES` membership (misses `ReturningDock`/`EmptyingDustContainer` detours → false positives). Flag stored in `AreaManagementService` (proxied via `RoborockService`), not a module-level var.
- Vacuum→Matter mapping (Jul 2026): bundle ideas 2–6 in `VacuumStatus.ts`; export `VACUUM_ERROR_TO_MATTER`; unknown non-zero → `UnableToCompleteOperation`; add `VacuumErrorCode.AutoEmptyDockFanError=33`; 8 semantic refinements per spike answer.md.
- `dss` field `isUpdownWaterReady` (bits 0-1) does NOT reliably follow the shared Unknown/Error/OK convention on real hardware: dss=2729 shows it=1 (Error) persistently while idle/charging with zero other error signal — false positive. Excluded from `hasError()`/`DSS_FIELD_PRIORITY` (Jul 22, 2026 fix); still parsed, just not error-consulted. Prior Item-3 inclusion (Jul 2026) was only "medium confidence" (ioBroker label, no RVC enum) — now disproven. Re-check (Jul 22): python-roborock `v1_containers.py:277-278` is `hatch_door_status` (bits 12-14), NOT bits 0-1 as previously miscited — python-roborock parses no field at bits 0-1 at all, any version.
- ThinQ CLI login helper (Sep 8, 2026): `src/cli.ts`+`src/cli/` mirrors Roborock's CLI shape but is a NEW, standalone consumer of `AuthenticationCoordinator`/`AccountAuthStrategy`/`TokenAuthStrategy`/`ThinqApiClient` (same ctor args as `ThinqServiceContainer`) — bypasses `UserDataRepository`/node-persist/`PlatformConfigManager` entirely so it runs with zero platform bootstrap. Own flat `.cli-session.json` (gitignored) instead.
- ThinQ AC command wiring (Sep 9, 2026): NO custom Behavior subclass needed — `@matterbridge/core`'s `MatterbridgeOnOffServer`/`ThermostatServer`/`FanControlServer` already dispatch bare `addCommandHandler('on'/'off'/'setpointRaiseLower')` before running `super.<cmd>()` (attribute mutation). Only `FanControl.percentSetting` and `Thermostat.occupiedCoolingSetpoint`/`occupiedHeatingSetpoint` bypass Behavior overrides entirely (raw attribute-write) — need `subscribeAttribute` + `context.fabric===undefined` guard to avoid a feedback loop with our own state-sync `updateAttribute` writes. See `workspace/thinq-ac-command-wiring/plan.md`.
- ThinQ `sendCommand` command field: default `'Set'`, but AC power needs `command: 'Operation'` (homebridge-lg-thinq parity) — `ThinqCommandPayload` widened with optional `command?: string`, spread order already lets it override.
- ThinQ AC command wiring implemented (Sep 9, 2026): `THINQ_FAN_SPEED_*` constants live in `thinqAirConditionerCommandHandlers.ts` (moved+exported, +`HIGH=6`); `thinqDeviceConfigurator.ts` imports them back for `mapWindStrengthToFanMode` (now exported). `thinqAirConditionerStateSync.ts` must import the constants directly from the command-handlers file, NOT re-export through `thinqDeviceConfigurator.ts` (`tsc` TS2459 — non-exported locals aren't transitively visible via re-export chains).
- ThinQ AC per-device capability gating (Sep 9, 2026): `AirConditioner`'s constructor (`@matterbridge/core/dist/devices/airConditioner.js`) unconditionally builds full Heating+Cooling+AutoMode Thermostat + Auto+Step FanControl — CANNOT be narrowed after construction. `Behaviors.require()` (`@matter/node/src/endpoint/properties/Behaviors.ts:310-321`) THROWS on a 2nd call for the same cluster id (sibling `.with(features)` types are never mutually `instanceof`-compatible, confirmed via `ClusterBehaviorType.ts`). Reduced-feature devices MUST be hand-composed via `MatterbridgeEndpoint` primitives (`createDefault{Heating,Cooling}ThermostatClusterServer`, `create{OnOff,Base,MultiSpeed,Complete}FanControlClusterServer`) chosen per-device BEFORE any `require()` for that cluster — exact tiered pattern already demonstrated in `matterbridge-example-dynamic-platform/src/module.ts:1920-1980` (thermostat tiers) and `:2100-2230` (4 fan tiers).
- `roomAirConditioner`/`powerSource`/`MatterbridgeEndpoint` are exported from the `matterbridge` package ROOT only (`@matterbridge/core/dist/export.js:27,29`), NOT from the `matterbridge/devices` subpath (that subpath only re-exports device _classes_ like `AirConditioner`).
- Matter's `Thermostat.Feature` has no Dry/FanOnly flag — `SystemMode.Dry(8)`/`FanOnly(7)` are ungated plain enum values; `ThermostatServer#assertSystemModeChanging` (`ThermostatServer.ts:615-634`) only forbids Heat/Cool crossing `controlSequenceOfOperation`, never Dry/FanOnly — Dry-gating is code-only (state-sync must never emit it), not a Matter feature-map concern. This same guard DOES throw `ConstraintErrorError` synchronously on `updateAttribute`-driven writes (not just user writes) if state-sync ever pushes `Heat`/`Auto` onto a `CoolingOnly` endpoint — remap defensively.
- FanControl's `percentSetting`/`percentCurrent` are unconditionally mandatory (`conformance:"M"`, `@matter/model/.../fan-control.element.ts:33-67`) regardless of `Feature.Step` — matter.js does NOT block a percent write on a reduced/no-feature FanControl cluster; the plugin's own command handler must add the capability guard, no framework help exists. `FanModeSequence` has exactly 6 members: `OffLowMedHigh=0, OffLowHigh=1, OffLowMedHighAuto=2, OffLowHighAuto=3, OffHighAuto=4, OffHigh=5`.

- `overrideMatterConfiguration` port planned (Sep 19, 2026, simplified vs Roborock): reuses existing
  `ThinqDeviceConfigEntry.productName?` (sibling of `capabilities`/`sceneButtons`), NOT a parallel
  `deviceProductNames[]` array. No `enableAdvancedFeature` flag exists in this repo (verified via grep,
  zero hits) — schema gates on `settings.overrideMatterConfiguration===true` alone via `dependencies`
  (this repo's own idiom, matching `thinq.dependencies.loginType`), not `allOf`. See `workspace/override-matter-configuration/plan.md`.
- Energy layout (current since Sep 20, 2026): when `supportsEnergyMonitoring`, the AC endpoint itself has device types [roomAirConditioner, powerSource, electricalSensor] + PowerTopology + ElectricalPowerMeasurement (activePower starts 0, not null); no `EnergyMonitor` child.
  Why: live iOS 27 tests showed the Energy tab and per-device detail only work with this layout AND a bridged AC (hardcoded `mode = 'server'` removed; server mode showed nothing); the old child layout gave only the whole-home total, blank detail.
- Partial LG MQTT push fix (Sep 19, 2026): state sync writes each Matter attr only if its raw key exists (`ThinqSnapshot.has`); systemMode w/o `airState.operation` derives power from current Matter `OnOff` (else skip); `onCurrent` = watts (no /100). See `workspace/partial-push-and-power-scale/plan.md`.
- ThinQ Washer v1 planned (Sep 22, 2026, read-only): Matter maps to `@matterbridge/core`'s ready-made `LaundryWasher` class (`matterbridge/devices`) used directly (`new LaundryWasher(name, id)`, zero hand-composition — no capability-gated feature tiers needed, unlike AC). `isWasherDevice()` scoped to exactly `type==='WASHER'` (deviceType 201) — `WASHER_NEW`/`WASH_TOWER`/`WASH_TOWER_2` stay unsupported (223 uses a different raw snapshot prefix `washer.*` vs `washerDryer.*`, unverified). All write commands (on/off/pause/stop/start/resume/changeToMode) `throw` inside `addCommandHandler` — `CommandHandler.executeHandler` has no try/catch so a throw aborts before local Matter attribute mutation (verified in `@matterbridge/core/dist/matterbridgeEndpointCommandHandler.js`). No washer capability-flag config/schema in v1 (no variable Matter feature set to gate). See `workspace/washer-integration/plan.md`.
- **CORRECTION to the above (Sep 22, 2026, verified directly against installed matterbridge 3.10.9 `matterbridgeBehaviors.js`/`NamedHandler.js`, not the file the v1 entry cited):** command dispatch (`on`/`off`/`pause`/`stop`/`start`/`resume`/`changeToMode`, all of them) calls `commandHandler.executeHandler(...)` WITHOUT `await`, then immediately mutates local Matter state and returns success — a throw in our async handler does NOT block mutation or block the success response to the controller. Any handler that must guarantee "no optimistic UI state on failure" needs an explicit revert-on-failure (`updateAttribute` back to the prior value in a `.catch()`), mirroring `revertSetpointOnFailure`/`revertToggleOnFailure` already in `thinqAirConditionerCommandHandlers.ts`/`thinqAirConditionerAuxiliaryToggles.ts` — a throw alone is not a safety mechanism.
- ThinQ Washer v2 planned (Sep 22, 2026, real control — plan-v2-control.md): scope narrowed to ONE real command, `OperationalState.stop`, double-gated (per-device config opt-in default-off AND a `ControlWifi.WMStop`/`WMOff` payload resolved from the device's own `modelJsonUri` at registration time, fetched via new `ThinqApiClient.getDeviceModel()`). `start`/`changeToMode` confirmed structurally inseparable from a course `dataSetList` payload in `ioBroker.lg-thinq` (`main.js:3289-3306`, `helper.js:1316-1405`) — no safe path without new course-table infra. No `pause` primitive exists at the ThinQ API layer at all (exhaustive grep, zero hits) — `pause`/`resume` stay permanently rejected, not aliased to stop/start. See `workspace/washer-integration/answers-washer-v2-control.md`.
- ThinQ Washer v2 Stop + v1 revert-on-throw implemented (Sep 22, 2026): `OperationalState.id`/`LaundryWasherMode.id` (and every other `matterbridge/matter/clusters` cluster namespace, e.g. `OnOff.id`) export `id` directly on the namespace in the installed matterbridge 3.10.9 (`@matter/types` re-export) — NOT nested under `.Cluster.id` as some upstream matter.js source suggests; use `<Cluster>.id` directly, matches existing codebase usage. `updateAttribute`'s 3rd param (`value`) must be typed as the attribute's real type (`number`/`boolean`), not `unknown`/`unknown|undefined` — `unknown` fails all 3 overloads (TS2769); read via `getAttribute(...) as T | undefined` then `?? fallback` before passing in.
- ThinQ AC filter monitoring planned (Sep 23, 2026, verified directly against installed SDK):
  `HepaFilterMonitoring` (id 113/0x71) is an OPTIONAL serverCluster directly on the `RoomAirConditioner`
  device type (`room-air-conditioner-device.element.js`, conformance "O") — no child endpoint/AirPurifier
  device type needed, unlike humidity/air-quality sensors. `MatterbridgeEndpoint.createDefaultHepaFilterMonitoringClusterServer(condition=100, changeIndication=Ok, inPlaceIndicator=true, lastChangedTime=null, replacementProductList=[])`
  already exists (same family as `createDefaultRelativeHumidityMeasurementClusterServer`), hardcodes
  `degradationDirection: Down` (matches "remaining life %", decreasing = worse). The fabricated
  "FilterMaintenance" (0x0050) cluster from an earlier bad AI answer does not exist in the SDK at all.
  Data source: `ThinqApiClient.sendCommandAndGetResponse` (already added for `--probe-filter` CLI flag)
  POSTs `control-sync` with `ctrlKey:'filterMngStateCtrl', command:'Get'` — REQUEST uses singular
  `airState.filterMngState.*` keys, but the RESPONSE comes back keyed with PLURAL
  `airState.filterMngStates.useTime`/`.maxTime`/`.changeDate` (confirmed live, `remainTime`/`type`
  requested but never returned — compute remaining as `maxTime-useTime`). See `workspace/ac-filter-monitoring/plan.md`.
- ThinQ AC filter monitoring implemented (Sep 23, 2026): `ThinqFilterState.fromRaw` returns `undefined`
  (not zeroed) when `useTime`/`maxTime` missing or `maxTime<=0`; `ThinqFilterMonitoringService` is a
  standalone interval-timer service (separate from `ThinqDeviceService`), `serviceContainer.getFilterMonitoringService()`
  singleton; `applyThinqFilterStateToAirConditioner` (new file, not `thinqAirConditionerStateSync.ts`)
  writes `HepaFilterMonitoring.condition`/`changeIndication` only when state is defined.

## Test Patterns

- **ThinQ partial push presence-gating tests (Sep 19, 2026):** `ThinqSnapshot.has(key)` returns true iff `data[key]` is neither undefined nor null (falsy values like 0, false, '' still count as present). `applyThinqSnapshotToAirConditioner` gates each Matter attribute write on `snapshot.has(sourceKey)`: operation required for OnOff/0-write, operation+opMode for systemMode (or opMode+OnOff-current-true for power-derived mode), windStrength for fanMode, swing axes for rockSetting. systemMode derives from opMode only when: operation=0 (→ Off), operation=1+opMode present (→ mapped), operation=1 only (→ skip), opMode only+OnOff true (→ mapped), else skip. Energy: operation present gates the zero-when-off fallback. Debug log two-line format: first lists pushed attributes, second lists skipped. `applyAuxiliaryToggleSnapshot` skips toggle when spec.dataKey absent from snapshot.
- **CLI utility tests (parseArgs, maskSecret, saveSession):** pure functions/I/O mocks — `parseArgs` handles flags/values, boolean flags (`--debug` → `'true'`), consumes next arg if not flag-prefixed; `maskSecret` preserves length, returns empty for falsy, fully masks if `value.length <= visibleChars`, else `'*'.repeat(length-visible) + value.slice(-visible)`. `saveSession` via `vi.mock('node:fs')` + spy `fs.writeFileSync`; verify formatted JSON (2-space indent) matches round-trip. Don't test edge cases (e.g. `visibleChars=0`) unless in plan.
- **ModeResolver.createDefaultModeResolver/createSmartModeResolver tests:** gated feature branches (e.g., OneTime→VacFollowedByMop mode 11) require two test paths — unsupported configs array (baseCleanModeConfigs, smartPlanModeConfig only) + supported array (inject vacFollowedByMopModeConfig). Test both branches and priority order (OneTime before isCustomMode before exact-match). Serialization key is `suctionPower:waterFlow:mopRoute` (sequenceType omitted).
- **RoborockServiceAreaServer.selectAreas override tests:** spy on parent prototype's selectAreas via `vi.spyOn(Object.getPrototypeOf(Object.getPrototypeOf(server)), 'selectAreas')` and mock its response to avoid deep matterbridge machinery; verify override calls `device.resolveAllRoomsForActiveMap()` for empty input, `device.trySwitchMap(areas)` for non-empty input, forwards resolved/original request to super with correct arguments.
- **Status listener error-code tests (V1StatusListener, handleHomeDataMessage):** listener fires `onError` whenever error-related field is _defined_ (including 0/None), not only non-zero; test three cases: payload with None/0 codes, field-absent (no call), sequential error→clear (both called). For home-data, `updateRobotWithPayload(ErrorOccurred)` only if errorCode defined; missing field never dispatches.
- **handleCleaningWithoutInfo + resolveV1CurrentArea tests:** test realistic multi-room `selectedAreas = [0, 1, 2...]` (not empty arrays) to match real-world scheduled-clean scenarios. Verify V1 resolution attempts before falling back to pin-first-area. Cover: fresh cache (resolves correct room), cache miss (pin-first + refresh), segment outside selectedAreas (pin-first + no refresh), non-V1 protocol (pin-first + no V1 calls), single area (immediate pin, no resolution), zero areas (null + empty).
- **DockStationStatus tests:** parse raw dss integers, 5-field priority in `hasError()`/`getMatterOperationalError()` (clearWater→dirty→dustBag→cleanFluid→filter, EXCLUDE isUpdownWaterReady). `isUpdownWaterReady` still parsed but false-positive on real hardware (dss=2729 idle = bit Error), verified via real-log fixture. Use `createDockStatus()` helper + `OK`/`ERR` constants for override tests.
- **resolveAreaFromCleaningInfo falsy-zero tests:** test areaId=0 separately (segment→0 resolves as 0, not null); verify `mappedArea === undefined` check (not `!mappedArea`). RoomIndexMap mock: multi-area roomMapData + roomInfo for segment→areaId mapping.
- **handleCleaningWithoutInfo last-known-area tests:** mock `getAttribute(ServiceArea.id, 'currentArea')` to return prior value; test first-update (undefined → fallback selectedAreas[0]), mid-run valid (→ reuse), mid-run falsy-zero (0 in selectedAreas[0,1,2] → use 0), stale value (not in selectedAreas → fallback). Use separate mockImplementation for each currentArea return value.
- **ModeResolver.resolveFallback category-aware tests:** import `vacuumOnlyModeConfigs`, `mopOnlyModeConfigs`, `vacuumAndMopModeConfigs` alongside `baseCleanModeConfigs`; test Max/Balanced/Quiet suction with non-canonical water/route → match same-category mode (not Default); test vacuum-off/mop-off branches; test unrecognized suction → Default fallback. Exact-triple match still takes priority (no regression).
- **LegacyMapParser tests (virtual walls/zones, containment tier):** V1 block decoders via `buildWallBlock`/`buildZoneBlock` (8-byte-per-wall, 16-byte-per-zone stride); truncated buffers drop partial entries. Bounding-box containment tier (new Jul 28): tests verify 5 scenarios — (1) containment wins over numerically-closer centroid, (2) ambiguous 2+ matches fall back to nearest-center, (3) zero matches fall back to nearest-center, (4) exact-pixel match still takes priority, (5) namedRooms name lookup works through containment path. Fixtures use multi-pixel segments to test bbox containment; robot mm→pixel conversion uses Y-flip: `pyRel = height - 1 - (pyAbs - top)`. Island detection: 4-connectivity BFS only (diagonal non-adjacent), wall tolerance ±3px, size-heuristic 30% threshold per-island (not map-wide).
- **V1 room-detection debounce tests (AreaManagementService.setV1ResolvedSegment/getV1ResolvedSegment, Aug 3, 2026):** debounce requires 2 consecutive matching calls to confirm; test matrix: (1) single call → undefined (not published), (2) two same → published, (3) two-matching + one-outlier → still first value (outlier doesn't reset published, only resets pending counter), (4) outlier + two-matching-new → new value published, (5) all-different sequence → never published, (6) clearAll() resets both pending+confirmed maps (test single call before/after clear), (7) per-duid isolation. Existing TTL tests must call setV1ResolvedSegment twice before time assertions.
- **ThinQ AC capability-gating tests (Sep 14, 2026):** `resolveAirConditionerCapabilities` pure resolver function tests cover: undefined/empty array → all-true default, device not found → default, missing capabilities key → default, mixed true/false per-device, multiple devices isolated resolution. `mapOperationModeToSystemMode` regression parity critical: default capabilities must produce identical SystemMode for every opMode/isPowerOn combo vs pre-change behavior; test Heat/Dry/Auto remapping when unsupported. `registerAirConditionerCommandHandlers` capability gating: supportsHeat=false → no heat subscribeAttribute (assert mock never called with attribute name), supportsFanSpeedControl=false → no-op on percent/fanMode writes + debug log. `applyThinqSnapshotToAirConditioner` parity + gating: full-support default parity assertion on all updateAttribute calls, heat setpoint skipped when unsupported, percentCurrent skipped when fan control unsupported. Minimal MatterbridgeEndpoint mock (log, addCommandHandler, subscribeAttribute, updateAttribute as vi.fn()) cast as `unknown as MatterbridgeEndpoint` to avoid 190+ property interface bloat.
- **ThinqServiceContainer singleton tests (Sep 16, 2026):** DI container getters (apiClient, getUserDataRepository, getAuthenticationCoordinator, getDeviceService, getDeviceDiscovery, getDeviceConfigurator, getMqttListener) all return fresh instances on first call, then same instance on repeated calls (singleton pattern, use `toBe` for identity check, not `toEqual`). Verify inter-instance wiring (e.g., `getDeviceDiscovery()` reuses same `ThinqDeviceService` as `getDeviceService()`) via bracket-notation private-field access; if public accessors exist, prefer those. Constructor parameters (logger, persist, configManager, mqttCertDir) flow through to constructed dependencies via mocks (`asPartial`).
- **ThinqSession tests (Sep 16, 2026):** Boundary-condition tests for expiry: `isExpired()` returns `false` at exact equality (check source: `expiresAtEpochSeconds < now`, so equal is NOT expired). Fake-timer setup required: `vi.useFakeTimers()` → `vi.setSystemTime(date)` → verify at boundary → `vi.useRealTimers()`. Round-trip serialization via `toData()` → `fromData()` must preserve all field values including falsy `''` accessToken. Static `expiryFromExpiresIn(seconds)` adds to current epoch (mocked via fake timers). `updateAccessToken()` mutates in-place (accessToken and expiresAtEpochSeconds), refreshToken immutable (`readonly`). All 4 hasValidToken boolean branches covered (empty+expired, empty+valid, has+expired, has+valid).
- **ThinqMqttListener.connect() happy path (Sep 16, 2026):** `aws-iot-device-sdk` mock requires constructor function returning mockDevice (not arrow function), placed at top-level `vi.mock()` block: `device: function() { return mockMqttDevice }` — enables `new awsIotDevice(...)`. Route → Key → CSR → Register → Certificate → ConstructDevice → WireEvents execution order verified via call tracking (not exact-time sequencing, simpler and clearer for this async flow). `wireMqttDeviceEvents` internally calls `device.on()`, verify via spy on that method. Separate happy-path tests from retry-exhaustion path (`start integration` tests) — split reduces mock fragmentation.
- **ThinqDeviceService.keepAliveOnce() tests (Sep 17, 2026):** Timer-based keep-alive tests using `vi.useFakeTimers()` + `advanceTimersByTimeAsync()`. `discoverDevices()` filters invalid device IDs via UUID regex (`isValidThinqDeviceId`), so test fixtures must use valid UUIDs (not strings like `'online-ac'`) or devices are silently dropped before `isAirConditionerDevice()` check, causing empty `airConditionerOnlineStatus` map. AC device filtering happens post-UUID validation. Error handling per-device (swallow + debug log, never throw out of interval callback), mirrors `pollOnce` pattern. Track `online` status in map during polling tick, reuse in keep-alive tick.
- **ThinQ AC Phase B swing-mode tests (Sep 18, 2026):** `isVerticalSwingOn`/`isHorizontalSwingOn` snapshot getters: value `100` (numeric) and `'100'` (string JSON cross-serialization) both return `true`; zero/absent/other → false (matches `isPowerOn` boolean-always contract, NOT optional-number). `supportsSwingMode` capability flag gates new `createCompleteFanControlClusterServer` call (vs. `createDefaultFanControlClusterServer`); test 3-way branch: (1) both `supportsFanSpeedControl` + `supportsSwingMode` true → Complete with Rocking features enabled + initial state false, (2) fan-speed true + swing false → Default (old behavior, regression), (3) fan-speed false + swing true → OnOff (swing silently ignored, no error thrown). `rockSetting` subscribeAttribute: handler guards on `supportsSwingMode` (mirror `percentSetting`'s guard pattern). Diff individual axes (vertical/horizontal) via old vs. new values, ignore rockRound-only changes. Compound `dataSetList` write (`ctrlKey:'favoriteCtrl'`) only when BOTH axes flip to true **simultaneously**; single-axis or asymmetric flips use individual writes. Handler ignores undefined new/old values. State-sync push: `rockRound` computed as `vertical && horizontal`, per reference behavior.
- **ThinQ AC Phase C sensor tests (Sep 18, 2026):** `humidityPercent` snapshot getter: value `<=100` → passthrough, `>100` → divide by 10 (heuristic for backend reporting in wrong scale). `airQualityOverall`/`pm25`/`pm10`: passthrough numbers or `undefined`. Capability flags `supportsHumiditySensor`/`supportsAirQualitySensor` default false (opt-in). Endpoint factory: `addChildDeviceType('HumiditySensor', [humiditySensor])` when humidity enabled, chained with `createDefaultRelativeHumidityMeasurementClusterServer(0)` and separate `createDefaultAirQualityClusterServer` + pm2.5/pm10 cluster servers when air-quality enabled; test via mock's `addChildDeviceType` spy filtering by child name. Regression parity: default capabilities (both false) yield zero child endpoint creation. State-sync push: test defensive child-exists check (getChildEndpointById may return undefined — no crash), value-undefined skip (even if capability true, don't push if snapshot value undefined), full parity with default capabilities on existing attributes (power/mode/temp/fan).
- **ThinQ AC Phase D energy monitoring tests (Sep 19, 2026):** `powerConsumptionWatts` getter returns raw `airState.energy.onCurrent` as real watts (no /100; raw 185 vs app 0.18 kW); key absent or NaN → undefined. State sync writes `Math.round(watts * 1000)` mW to `ElectricalPowerMeasurement.activePower` on the AC endpoint (no child), gated on `supportsEnergyMonitoring` (default false); since Sep 20 the write is 0 when AC is known off (snapshot `airState.operation` = 0, else endpoint OnOff exactly false) because LG reports a floor of about 50 when off.
- **ThinQ AC Phase F scene buttons tests (Sep 19, 2026):** Scene buttons config is NOT a capability flag (no `resolveAirConditionerCapabilities`), carried as `ThinqSceneButtonConfig[]` in config only. `addSceneButtonEndpoints`: sanitize each button's name via `replaceAll(' ', '')`, deduplicate by index if two buttons collide (e.g. "Power Off" + "PowerOff" both → "PowerOff", "PowerOff0"). `genericSwitch` device type + `createDefaultMomentarySwitchClusterServer()` per button (no other clusters). `registerSceneButtonCommandHandlers`: same deduplication, `addCommandHandler('toggle', ...)` per button (MomentarySwitch cluster's press event → 'toggle' command), sends `{ dataKey: 'airState.opMode', dataValue: button.opMode }` with standard error handling (no special gates like toggles). Endpoint factory receives buttons via optional `BuildAirConditionerEndpointOptions` param (not capability-gated, called unconditionally if non-empty). PlatformConfigManager getter: `getSceneButtons(deviceId)` → `config.thinq.devices.find(...).sceneButtons ?? []` (mirrors getDeviceCapabilities shape).
- **ThinQ token-refresh dual-trigger tests (Sep 19, 2026):** `ThinqApiClient.request()` retries on 401 OR HTTP 400 + `resultCode:'0102'` via single `isTokenExpiredError()` predicate. Test 5 parallel cases (vs. existing 401 cases): (1) 0102-retry-succeeds via `getListHomes`, stub refresh, assert resolves to homes; (2) 0102-retry-exhausted throws `TokenExpiredError` both calls return 400+0102; (3) `sendCommand`-to-control-sync 0102 self-heals, verify POST count = 2; (4) different `resultCode` (e.g. 0110) under 400 rejects without retry/refresh attempt; (5) malformed/absent body (400 with no data, or 400 + non-JSON string) doesn't crash, rejects with 1 GET call and 0 refresh calls. Use `mockAxios.history.post.filter((h) => h.url?.includes('oauth2/token'))` length assertion to prove (no-)refresh attempt.
- **ThinQ Washer command handler extraction pattern (Sep 22, 2026):** Mock handler callbacks from `vi.mocked(mockEndpoint.addCommandHandler).mock.calls` require explicit type annotation `(call: any[])` on `.find()` / `.filter()` callbacks (fixes TS7006 implicit-any errors). Replace non-null assertion `![1]` with optional chaining + explicit cast: `const stopCall = vi.mocked(...).find((call: any[]) => call[0] === 'stop'); const handler = stopCall?.[1] as () => Promise<void>;` — avoids forbidden non-null operator. CLI function argument expansion (3-arg cmdDevices): test both no-flag (third arg = undefined) and with-flag (third arg = flag-value) paths separately.
- **ThinqDeviceConfigurator.registerAirConditioner regression test (Sep 20, 2026):** No hardcoded `mode: 'server'` assignment — mock endpoint pre-seeded with `mode: 'server'` was removed, test asserting `result.mode === 'server'` was removed. NEW regression test: endpoint returned from `registerAirConditioner` has `mode` undefined (proving the function doesn't set it). Mock endpoint factory removed the seed and only returns chainable mock methods (`createDefaultTemperatureMeasurementClusterServer`, `addRequiredClusterServers`). Existing capability/handler-wiring tests unaffected.

## Common Pitfalls

<!-- Things to avoid — bugs found, anti-patterns, footguns -->

- `handleCleaningWithoutInfo`'s `selectedAreas[0]` pin (`serviceAreaHandler.ts:311`) DOES reuse last-known `currentArea` first (PR #150/ef1efb1) via `robot.getAttribute(ServiceArea.id,'currentArea',...)` — this is the ONLY call site of that read in `src/`; re-verified Aug 3, 2026 against a fresh real-S8 log: all 34 occurrences match the prior write correctly. A prior note claiming this read was unreliable and had an Aug-25 `AreaManagementService.lastKnownAreaCache` fix was WRONG — that field never existed in `src/` (confirmed via grep) — do not rely on it existing.
- `resolveAreaFromCleaningInfo`'s falsy-zero check is fixed (`mappedArea === undefined`, `serviceAreaHandler.ts:405`) — confirmed correct Jul 25, no gap.
- `DockStationStatus.isUpdownWaterReady` (bits 0-1 of dss) is parsed/observable but EXCLUDED from `hasError()`/`getMatterOperationalError()` (Jul 22, 2026 fix, `rvc-unable-to-complete-cleaning-error`). Real hardware shows it=1 (Error) as steady-state idle/charging, not transient fault — unreliable error signal, unlike the 5 other dss fields.
- `modeResolver.ts`'s `createDefaultModeResolver`/`createSmartModeResolver` `customCheckFn` OneTime branch (pre-fix Jul 22) unconditionally returned `VacFollowedByMop` mode (11) regardless of `configs` — real `seq_type` wire field (`v1StatusListener.ts:96`) reaches it on ANY non-B01 device (`connectionService.ts:161`), not just capability-gated ones. Fix: gate on `configs.some(mode===11)`, computed once per factory call (closures already have `configs` in scope, no signature change).
- CORRECTED (Sep 17, 2026): prior "Phase 1 auth fields not exposed in schema.json" note was stale — fixed same-day by commit `7366426` (`dependencies.if/then/else` block, mirrors roborock's `authenticationMethod` pattern). Schema/config.json now fully sync w/ `LgThinkqPluginPlatformConfig.ts`; verified field-by-field Sep 17. See `workspace/schema-config-gap/plan.md`.
- `refreshIntervalSeconds` default mismatch (Sep 17, 2026): `platformConfigManager.ts:16` code fallback = 5s, but `schema.json`/`config.json` advertise `default:60`. Only bites if the field is truly absent from saved config (not a normal-UI-path issue) — flagged, not fixed.
- AC keep-alive gap confirmed real (Sep 17, 2026): reference's `temperatureKeepAliveCommandForDevice` (`AirConditioner.ts:644-661`) POSTs `service/devices/{id}/control` w/ `ctrlKey:'allEventEnable'`, `dataKey:'airState.mon.timeout'=70`, every 60s — DIFFERENT call than the GET-based poll (`platformMonitor.ts:104-123` runs both concurrently, proving non-redundancy). This repo's poll (`thinqDeviceService.ts` `getListDevices`, GET only) does NOT cover it — zero hits for `allEventEnable`/`mon.timeout` in `src/` pre-fix. See `workspace/ac-keepalive-heartbeat/plan.md`.
- ThinQ MQTT push (Phase 4, Sep 15, 2026 plan): reference (`homebridge-lg-thinq`) uses `aws-iot-device-sdk` (^2.2.16) + `node-forge` (^1.4.0) for cert-based AWS-IoT MQTT, NOT the generic `mqtt` npm package — confirmed via reference `package.json`. Cert flow: `GET https://common.lgthinq.com/route` → `POST service/users/client` → `POST service/users/client/certificate` (csr) — all through the SAME authenticated `ThinqApiClient.request()` (absolute URIs pass through `new URL(uri, base)` unaffected). Root-CA PEM download (amazontrust/sectigo/digicert) is a plain unauthenticated GET — reference attaches ThinQ auth headers to it too (harmless but unnecessary), our port deliberately skips that.
- ThinQ MQTT message shape confirmed identical to REST snapshot shape: `{ deviceId, data: { state: { reported: {...} } } }`, and `reported` is the same flat `airState.*`-keyed bag `ThinqSnapshot`'s constructor already accepts — zero new mapping logic needed, both polling and MQTT feed the exact same `ThinqDeviceUpdateListener` callback.
- ThinQ MQTT push implemented (Sep 15, 2026): `services/thinq/mqtt/{mqttCertificate,mqttConnectionEvents,mqttRetry,mqttKeyRepository,thinqMqttListener}.ts` (new) + `thinqApiClient.ts`/`serviceContainer.ts`/`module.ts` wiring, per `workspace/thinq-mqtt-push/plan.md`. `MqttRuntimeDevice.on()` needed `'connect' | 'offline'` combined into ONE overload signature — ESLint `@typescript-eslint/unified-signatures` fails otherwise (2 overloads differing only in event-name literal, same handler shape).
- `npm install` (plain, no args) SILENTLY REMOVES a prior `npm link matterbridge` symlink from `node_modules/matterbridge` (matterbridge isn't a `package.json` dependency, only linked) — any implementer adding a new npm dependency mid-task must `npm link matterbridge` again afterward, or `type-check:ci`/`tsc` floods with `Cannot find module 'matterbridge'` across the whole repo (false-looking whole-codebase break, actually just the missing link).

## Module Notes

<!-- Notes about specific modules, non-obvious behaviors -->

- `roomNameNormalizer.ts` (b01): pure module, no imports. `normalizeB01RoomName(roomName, roomTypeId?, roomId?)` returns non-empty; callers may still chain `|| fallback`. `rr_other` (typeId 0) always resolves to `Room ${roomId}`.

- Q10 map format: python-roborock's `lz4_block_decompress` (`b01_q10_map_parser.py:190-237`) is a hand-rolled zero-dep LZ4 _block_-format decoder, NOT a call into the Python `lz4` package — no size param, decodes until input exhausted.
- ioBroker's Q10 parser lives at `ioBroker.roborock/src/lib/map/q10/Q10YxMapParser.ts` (not `b01/`) — a structurally different "YxMap" format (28-byte header, version/pixLen/pixLzLen fields) vs python-roborock's `01 01`+offset-27/29 layout; only LZ4-block-format + big-endian u16 width/height are cross-corroborated.
- Q10 fix landed: `b01MapParser.ts.parseRoomsFromEncryptedBinary` routes `0x01 0x01`-prefixed payloads to new `b01Q10MapParser.ts`/`lz4BlockDecompressor.ts` (hand-rolled, zero-dep); Q7 AES+zlib pipeline untouched. `0x02 0x01` trace packets now route to `b01Q10TraceParser.ts` (`parseQ10TracePacket` → `B01MapInfo.currentPose` = last point; rooms/mapId/roomMatrix always empty/undefined) instead of falling through to the Q7 path — superseded a same-day minimal "recognize+skip, no parse" fallback that had landed on `dev` first.
- LG ThinQ washer snapshot shape (Sep 22, 2026, cross-checked homebridge-lg-thinq + ioBroker.lg-thinq): raw key prefix `washerDryer.*` for deviceType 201/221/222, but `washer.*` (remapped) for 223. Fields confirmed read: `state` (raw string enum: POWEROFF/POWERFAIL/RUNNING/PAUSE/ERROR/END/etc, NOT model-resolved), `remainTimeHour`/`remainTimeMinute` (zeroed when not running), `doorLock`/`remoteStart` (DeviceModel-lookup enums), `TCLCount` (maintenance counter). No course/cycle/spin-speed/water-temp fields read by either reference. `homebridge-lg-thinq`'s washer is 100% read-only (zero `deviceControl()` calls); `ioBroker.lg-thinq` DOES send washer commands (course download/start/stop via a heavy model-JSON-driven `dataSetList` builder, `lib/helper.js:1316-1405`) but has no TS precedent and no local sample model JSON to verify against. See `workspace/washer-integration/answers-washer-reference.md`.
- `0x02 0x01` confirmed = python-roborock `Q10TracePacket` (10-byte header + repeating i16be x/y pairs, robot_position=last point); NOT corroborated by ioBroker (different wire format, no `02 01` marker at all). Real-hardware repro: 598B during active cleaning vs 7076B full map. Live-validated on real Q10 S5+: real changing (x,y) coordinates, zero crashes across 8 invocations. `mapInfoListener.ts`/`roomMatrixResolver.ts` needed zero changes (already consume `currentPose` generically); area/room wiring stays out of scope.

- ThinQ token-refresh dual-trigger (Sep 19, 2026, `thinq-resultcode-0102-fix`): `ThinqApiClient.request()`'s reactive refresh-and-retry-once now fires on HTTP 401 OR HTTP 400 body `resultCode:'0102'` (LG's own expiry signal, `control-sync` uses this shape) via one `isTokenExpiredError()` predicate — mirrors `homebridge-lg-thinq/src/lib/request.ts:80-97`. Co-located `thinqApiClient.test.ts` already covered 401 retry; extended with parallel 0102 cases.
- `FanControl` cluster (Matter model, `@matter/model/src/standard/elements/fan-control.element.ts:33-67`): `PercentSetting`/`PercentCurrent` are conformance `"M"` (mandatory, unconditional) — NOT gated by any Feature bit. Only SpeedMax/Setting/Current (MultiSpeed), Rock*(Rocking), Wind*(Wind), AirflowDirection(AirflowDirection), and the `Step` command itself (Step feature) are feature-conditional.
- `createOnOffFanControlClusterServer` (zero Feature flags) still fires `subscribeAttribute(FanControl,'percentSetting',...)` normally — matter.js has no protocol-level rejection for feature-less fan writes; caller must self-guard. Proven both by conformance model and live example (`matterbridge-example-dynamic-platform/src/module.ts:2163-2196`, `fanOnHigh`). `FanModeSequence` enum has exactly 6 members (0-5): OffLowMedHigh, OffLowHigh, OffLowMedHighAuto, OffLowHighAuto, OffHighAuto, OffHigh.
- `matterbridge-example-dynamic-platform/src/module.ts:1920-1980,2100-2230` has a full reduced-feature demo block: heating-only/cooling-only thermostats + 4 fan tiers (default/base/on-off/complete), each a manual `MatterbridgeEndpoint([fan,...])` composition with its own subscribeAttribute clamp/guard — reusable pattern for per-device Matter feature gating.

## Open Questions

<!-- Unresolved questions for the team -->

- Does our TypeScript plugin call `APP_GET_INIT_STATUS`? If so, are `newFeatureInfo`/`newFeatureInfoStr`/`featureInfo` captured and stored?
- `roomMatrix` (RobotMap field 13) confirmed undecoded in python-roborock/ioBroker.roborock/roborock-gitlab too — all define it, none decode it (room-pixel data comes from `roomChain`/occupancy grid instead). Still needs real Q10 packet capture.
- `OperationStatusCode` 104: confirmed absent from canonical status enum in all 3 reference repos (identical `103→202` gap everywhere). The only "104" found is an unrelated DP-id (`BREAKPOINT_CLEAN`), not a status value — coincidence, not the answer.

- Coverage-gate test-plan (Sep 16, 2026): `src/module.ts` (`LgThinkqMatterbridgePlatform`) stays an accepted gap a 3rd time — `vi.mock('matterbridge')`-stubbing `MatterbridgeDynamicPlatform`'s base-class chain (protected ctor, `@matterbridge/core`) is theoretically viable (smaller surface than `MatterbridgeEndpoint`) but unproven and not required: Tiers 1-4 in `workspace/coverage-threshold-gap/test-plan.md` alone clear the 75% floor (+71 lines/+70 stmts/+16 fn/+32 branches needed).
- Repo has TWO test-file layouts, both current: colocated (`src/services/**/*.test.ts`, e.g. `serviceContainer.test.ts`) and centralized (`src/tests/**/*.test.ts`, e.g. `cli/*`, some `platform/thinq/*`). Match the sibling directory's existing convention, don't invent a third.

## Archive

Older entries and one-off research (B01/Q7/Q10 current-room detection, featureSet decoding reference) live in `wiki/memory-archive.md` — consult it when working on those areas.
